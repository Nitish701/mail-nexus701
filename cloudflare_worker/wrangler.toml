name = "email-detector"
main = "worker.js"
compatibility_date = "2026-09-20"

[vars]
WEBHOOK_URL = "https://api.edushield1.in"
# Set WEBHOOK_SECRET with `wrangler secret put` only when the backend requires one.